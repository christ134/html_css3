var num=541;
var rem=0;
var rev=0;

var revno=palindrome(num,rem,rev)

function palindrome(x,y,z)
{
    while(num>0);
    {
        y=x10;
        z=y+z*10;
        x=parseInt(x/10);
        return z;
    }
}

console.log(revno);